on:
  push:
    branches:
      - main
jobs:
  build:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Create new tag and release
        uses: ./release
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}